//! Explorer panel — stub on the viewers track.
//!
//! The real panel (left tree + detail pane + dispatch glue) is authored
//! by the panel worker on branch `phase1-explorer-panel` and will
//! REPLACE this stub on merge. This module exists only so the viewers
//! track (`viewers/` sub-tree) compiles standalone on
//! `phase1-explorer-viewers`.

pub mod viewers;
