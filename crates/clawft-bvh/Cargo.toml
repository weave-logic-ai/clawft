[package]
name = "clawft-bvh"
version.workspace = true
publish = false
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "BVH broad-phase spatial index (ADR-056 Phase A). No kernel/chain deps — pure geometry + registry."

[dependencies]
serde = { workspace = true }
# Payload VectorRef decode for Phase F join (WEFT-723 / ADR-093).
weftos-leaf-types = { path = "../weftos-leaf-types" }

[dev-dependencies]
rand = { workspace = true }
