# WeftOS mapping + graduation verdict

**Verdict:** **sidecar, do not graduate to kernel this cycle.** Exploratory. Local `LDA-ADR` only. No Plane item until someone actually attaches the contract to one live eval.

## Landings

| Primitive | Surface | How | ADR |
|-----------|---------|-----|-----|
| circuitus | every published score | sidecar `circuit` | LDA-001 |
| aequitas | EffectVector.fairness | check beside, not instead | LDA-002 |
| odds / edge | router, vendor, judge, opportunities | `edge` index | LDA-003 |
| lean / n | MH, Fitness, NodeScoring | `calibration` | (001 field) |
| receipts | flywheel | already; keep | — |
| remaining work | delegation / interrupt | continuation EV | LDA-005 |
| ruin | cost breaker | field beside breaker | LDA-004 |
| portfolio | AgentDB patterns | store n, not only recipe | practice |

## Will not do

Genesis 6th face; rename breaker; call fairness aequitas without the check; ROTM-as-prior; fake $; auto-promote.

## First three moves

1. Fix `governance-counsel.md` 5D drift (equal our own table).
2. Sidecar the contract on the next `metaharness_score` throw.
3. Leave genesis and Plane alone until (2) has been done once.

If (2) proves lift, recommend 0.8.x+ Plane item and promote LDA-001 to a real ADR.
