{
  "$schema": "https://schemas.tauri.app/config/2",
  "identifier": "default",
  "description": "Default capability for the ClawFT desktop shell (WEFT-228 mic + core shell).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "allow-mic-all"
  ]
}
